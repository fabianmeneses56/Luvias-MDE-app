/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import SiataApp from './src/SiataApp';

AppRegistry.registerComponent(appName, () => SiataApp);
